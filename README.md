# holbertonschool-interview

Solutions to technical interview practice problems.

## utf8_validation

UTF-8 validation algorithm.

- `0-validate_utf8.py` : method `validUTF8(data)` that determines if a
  data set represents a valid UTF-8 encoding.
