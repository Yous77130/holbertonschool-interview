# Lockboxes

A method that determines if all locked boxes can be opened.

## Prototype

```python
def canUnlockAll(boxes)
```

## Description

Given n locked boxes, each containing keys to other boxes,
determine if all boxes can be opened starting from box 0.

## Returns

- True if all boxes can be opened
- False otherwise
